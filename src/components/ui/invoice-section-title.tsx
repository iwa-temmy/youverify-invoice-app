const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h4 className="uppercase text-xs text-grey font-medium tracking-[.05rem]">
      {title}
    </h4>
  );
};

export default SectionTitle;
